---
base_workstation_base_packages:
  - ca-certificates
  - curl
  - git
  - python3
  - python3-apt
  - rsync
  - sudo
  - unattended-upgrades
  - wget

base_workstation_optional_packages:
  - htop
  - tmux
  - vim

base_workstation_extra_users: []

base_ansible_pull_repo_url: "https://github.com/ngoh10-hhmi/ansible-pull.git"
base_ansible_pull_branch: "main"
base_ansible_pull_playbook: "playbooks/workstation.yml"
base_ansible_pull_directory: "/var/lib/ansible-pull"
base_ansible_pull_log_dir: "/var/log/ansible-pull"
base_ansible_pull_timer_on_calendar: "daily"
base_ansible_pull_randomized_delay_sec: "15m"

base_apt_maintenance_enabled: false
base_apt_maintenance_timer_on_calendar: "Sat *-*-* 03:00:00"

base_workstation_enable_unattended_upgrades: true
base_workstation_enable_periodic_package_list_update: true

base_unattended_upgrade_origins_patterns:
  - 'origin=Ubuntu,archive=${distro_codename}-security'
  - 'origin=Ubuntu,archive=${distro_codename}-updates'

base_security_focus_notes:
  - "Ubuntu security updates are handled through unattended-upgrades."
  - "Packages installed from APT repositories can be updated automatically."
  - "Firefox on modern Ubuntu is typically a snap and is not managed by these APT tasks."
